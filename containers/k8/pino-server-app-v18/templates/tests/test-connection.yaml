apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pinoserver.fullname" . }}-test-connection"
  labels:
{{ include "pinoserver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pinoserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
