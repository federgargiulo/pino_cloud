# Default values for helloworld.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: hashicorp/http-echo
  pullPolicy: IfNotPresent
  tag: "0.2.3"




service:
  type: ClusterIP
  port: 5678

strategy: {}

ingress:
  ## If true, pgAdmin Ingress will be created
  ##
  enabled: true
  className: ''
  annotations:
    nemo.eu/ingress-expose: "true"
    external-dns.alpha.kubernetes.io/hostname: pino-helloworld-v5.workload.nemo.onelab.eu
  hosts:
    - host: pino-helloworld-v5.workload.nemo.onelab.eu
      paths:
        - path: /
          pathType: ImplementationSpecific
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  #hosts:
  #  - host: helloworld.local
  #    paths:
  #      - path: /
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: #{}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

serviceAccount:
  create: false