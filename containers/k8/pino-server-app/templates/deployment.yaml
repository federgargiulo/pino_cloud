apiVersion: apps/v1
kind: Deployment
metadata:
  name: pino-server-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pino-server-app
  template:
    metadata:
      labels:
        app: pino-server-app
    spec:
      containers:
        - name: pino-server-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            - name: JAVA_OPTS
              value: "{{ .Values.env.JAVA_OPTS }}"
            - name: SPRING_OPTS
              value: "{{ .Values.env.SPRING_OPTS }}"
          volumeMounts:
            - name: config-volume
              mountPath: {{ .Values.config.mountPath }}
              readOnly: true
            - name: logs-volume
              mountPath: {{ .Values.config.logsPath }}
      volumes:
        - name: config-volume
          configMap:
            name: pino-server-app-config
        - name: logs-volume
          emptyDir: {}
