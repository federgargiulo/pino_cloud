apiVersion: v1
kind: Service
metadata:
  name: {{ include "pino-server-app-v6.fullname" . }}
  labels:
    {{- include "pino-server-app-v6.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pino-server-app-v6.selectorLabels" . | nindent 4 }}
