apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pino-server-app-v6.fullname" . }}-test-connection"
  labels:
    {{- include "pino-server-app-v6.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pino-server-app-v6.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
